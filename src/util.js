function parseLogicalString(logicStr) {
	// & means something is required
	// | means it is one of a few options


	//parse until we hit ) or & or |
	//otherwise we will assume its a number aka an id

	
}